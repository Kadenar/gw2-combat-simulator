// Compatibility barrel for GW2 result presentation while callers migrate to the game package.
export { createGw2SimulationViewModel, gw2SimulationPresentation, renderResults } from '../../presentation.js';
