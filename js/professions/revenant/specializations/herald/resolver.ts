// Herald has no resolver-side event processing; all facet and invocation logic runs in the scheduler (rules.ts, upkeep.ts).
export const heraldEventHandlers = Object.freeze({});
export const heraldEventReactions = Object.freeze({});
