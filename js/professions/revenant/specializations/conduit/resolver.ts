// Conduit has no resolver-phase event handlers; all damage resolution uses the shared platform path.
export const conduitEventHandlers = Object.freeze({});
export const conduitEventReactions = Object.freeze({});
