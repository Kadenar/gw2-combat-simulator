/**
 * Renegade keeps this module as a specialization architecture boundary.
 * Patchable balance data lives in skills.ts and modifier-rule parameters.
 */
export const RENEGADE_MECHANICS = Object.freeze({});
