// Vindicator has no resolver-phase event handlers; all damage/buff logic runs in the scheduler.
// vindicatorEventReactions is declared but not wired into the module; kept for future expansion.
export const vindicatorEventHandlers = Object.freeze({});
export const vindicatorEventReactions = Object.freeze({});
