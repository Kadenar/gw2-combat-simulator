/**
 * Marks Core's non-patchable mechanic configuration boundary. Its current
 * runtime values are owned by skill declarations and balance profiles.
 */
export const NECROMANCER_CORE_MECHANICS = Object.freeze({});
