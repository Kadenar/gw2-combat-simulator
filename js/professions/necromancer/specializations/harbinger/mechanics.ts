/**
 * Marks Harbinger's non-patchable mechanic configuration boundary. Its
 * current runtime values are owned by skill declarations and balance profiles.
 */
export const HARBINGER_MECHANICS = Object.freeze({});
