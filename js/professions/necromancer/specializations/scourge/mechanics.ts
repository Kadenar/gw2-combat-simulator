/**
 * Marks Scourge's non-patchable mechanic configuration boundary. Its current
 * runtime values are owned by skill declarations and balance profiles.
 */
export const SCOURGE_MECHANICS = Object.freeze({});
